pipeline {
    agent {
       label "deploy"
    }
    stages {
          stage("connect") {
             steps {
               script {
                   sh '''
                        test_searching_real_coordinates.py
                   ''' 
               }
             }
          }
        }
}
