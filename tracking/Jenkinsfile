pipeline {
    agent {
       label "deploy"
    }
    stages {
          stage("connect") {
             steps {
               script {
                   sh '''
                       apt install python3-pip
                       pip install numpy
                       cd ./tracking
                       python3 -m unittest
                   ''' 
               }
             }
          }
        }
}
