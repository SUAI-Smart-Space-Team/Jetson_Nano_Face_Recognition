pipeline {
    agent {
       label "deploy"
    }
    stages {
          stage("connect") {
             steps {
               script {
                   sh '''
                       python ./tracking/test_searching_real_coordinates.py
                   ''' 
               }
             }
          }
        }
}
