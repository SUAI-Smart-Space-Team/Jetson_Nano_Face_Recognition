pipeline {
    agent {
       label "deploy"
    }
    stages {
          stage("connect") {
             steps {
               script {
                   sh '''
                       cd ./tracking
                       python3 -m unittest
                   ''' 
               }
             }
          }
        }
}
