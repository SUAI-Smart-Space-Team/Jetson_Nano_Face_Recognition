pipeline {
    agent {
       label "deploy"
    }
    stages {
          stage("connect") {
             steps {
               script {
                   sh '''
                       python test_searching_real_coordinates.py
                   ''' 
               }
             }
          }
        }
}
