pipeline {
    agent {
       label "jcworker"
    }
    stages {
          stage("connect") {
             steps {
               script {
                   sh '''
                   make image VERSION=jetson_site3 FILE=./jetson_site/Dockerfile BRANCH=null
                   make push VERSION=jetson_site3
                   make pull VERSION=jetson_site3
                   '''
               }
             }
          }
        }
} 
